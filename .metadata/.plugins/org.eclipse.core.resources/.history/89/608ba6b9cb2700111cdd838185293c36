package com.tus.characters.repository;

import static org.junit.jupiter.api.Assertions.assertNotNull;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.orm.jpa.TestEntityManager;
import com.tus.characters.entity.User;

public class UserRespositoryTest {

	 @Autowired
	 private TestEntityManager entityManager;
    @Test
    void createUser_CallsUserRepositorySave() {
        User user = new User();
        user.setUsername("newuser");
        user.setEmail("newuser@example.com");
        user.setPassword("password");

        User saved = entityManager.persistAndFlush(user);

        assertNotNull(saved.getUserId(), "User should be saved and have an ID");
    }
    
  /*  @Test
    void deleteUser_CascadesToCharacters() {
        User user = new User();
        user.setUsername("cascadeUser");
        user.setEmail("cascade@example.com");
        user.setPassword("password");
        user = entityManager.persistAndFlush(user);

        Character character = new Character();
        character.setLevel(10);
        character.setCharacterClass("Warrior");
        character.setCharacterRace("Human");
        character.setUser(user);
        entityManager.persistAndFlush(character);

        entityManager.remove(user);
        entityManager.flush();

        List<Character> characters = characterRepository.findByUser(user);
        assertTrue(characters.isEmpty(), "Characters should be deleted when user is removed");
    }*/
}
