package com.tus.characters;

import org.junit.jupiter.api.Test;

class CharactersApplicationTests {

    @Test
    void main_ShouldRunApplicationWithoutStartingServer() {
        // Call main() directly — this counts for coverage
        // but does NOT start Spring Boot web server
        CharactersApplication.main(new String[]{});
    }
}